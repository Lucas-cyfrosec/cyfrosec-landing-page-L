# Local development overrides
# Sign In / Go to Dashboard will navigate to the local app
NEXT_PUBLIC_APP_URL=http://app.localhost:8080

# API calls (contact form, book-demo, get-started signup) hit the local backend via nginx
NEXT_PUBLIC_BACKEND_URL=http://app.localhost:8080
